{
  "name": "rtcp-monorepo",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:web": "npm --workspace @rtcp/web run dev",
    "dev:realtime": "npm --workspace @rtcp/realtime run dev",
    "build": "npm run -ws build",
    "test": "npm run -ws test"
  }
}
